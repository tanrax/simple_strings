# simple_strings

``` python

from simple_strings import *

split_removing_spaces(text, cutter_element=" ")

get_random_token_int(length=6)
  
transform_to_snake_case(text)
  
transform_to_kebab_case(text)

transform_to_camel_case(text)
```
